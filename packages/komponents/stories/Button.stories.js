import React from 'react';

import { storiesOf } from '@storybook/react';

storiesOf('Button', module)
    .add('Test', () => 
        <button type='button' className='btn btn-primary'>Valuee</button>)
